# Database Foundation

This directory holds database implementation assets for MVP execution:

- `migrations/` for forward-only migration files
- `seeds/` for deterministic seed data

Not included yet:

- product schema implementation
- business-specific migrations
