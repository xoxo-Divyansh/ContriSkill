export default function ProtectedAppHomePage() {
  return (
    <main>
      <h1>Protected App Home</h1>
      <p>Protected route shell is active. Product feature screens are intentionally deferred.</p>
    </main>
  );
}
